create docckerfile: 
	FROM python:3.10
	WORKDIR ./app
	COPY . /app
	RUN pip install -r requirements.txt
	EXPOSE 5000
	CMD python3 ./app.py 
	
	
command to create docker image and push to repo
- docker build -t abdoes/flask-app:v1
- sudo usermod -aG docker ${USER} /// to add a user in docker 
- su - ${USER} /// to use docker properly and build docker image 


